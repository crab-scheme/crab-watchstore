
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Build / runtime artifacts
target/
result/
*.log
db/
*-shard*/
*.tmp
data/
snapshots/

# Compiled Go test programs (rebuilt via `go run`; never the prebuilt binary)
test/clientv3-compat/clientv3compat

# terraform plan artifacts
tfplan
*.tfplan
